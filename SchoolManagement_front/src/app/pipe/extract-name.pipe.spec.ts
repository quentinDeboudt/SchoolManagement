import { ExtractNamePipe } from './extract-name.pipe';

describe('ExtractNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ExtractNamePipe();
    expect(pipe).toBeTruthy();
  });
});
